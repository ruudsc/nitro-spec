{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Nitro: debug server-side",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm run dev",
      "cwd": "${workspaceFolder}/playground/nitro-app"
    }
  ]
}
